use std::net::{TcpListener, TcpStream};
use std::io::{BufReader, BufReade, Wite};
use std::thread;
use std::sync::mpsc;
use std::time::Duration;

pub fn main() {
    let server_addr = "127.0.0.1:8888";
}